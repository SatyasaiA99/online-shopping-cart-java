apiVersion: v1
kind: Service
metadata:
  name: onlineshopping-service
spec:
  type: LoadBalancer
  selector:
    app: onlineshopping
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
